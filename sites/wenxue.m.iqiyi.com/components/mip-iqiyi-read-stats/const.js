const PREFIX = 'iqiyi-read-stats'

export const RSEAT_ATTR = `${PREFIX}-rseat`
export const BLOCK_ATTR = `${PREFIX}-block`

export const OPTION_ERROR = '组件内部必须写入 JSON 格式参数，并且包含必选项 "config"'

export const THROTTLE_DELAY = 50
